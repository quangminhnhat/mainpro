<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notifications</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/mobile.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://demos.creative-tim.com/notus-js/assets/styles/tailwind.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
    <link rel="stylesheet" href="https://demos.creative-tim.com/notus-js/assets/vendor/@fortawesome/fontawesome-free/css/all.min.css">
    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.13.7/css/jquery.dataTables.css">
    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/responsive/2.5.0/css/responsive.dataTables.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

    <style>
        /* General layout */
        html, body { height: 100%; margin: 0; padding: 0; font-family: 'Inter', sans-serif; }
        body { display: flex; flex-direction: column; }
        main { flex: 1 0 auto; }
        footer { flex-shrink: 0; }

        /* Modern Container */
        .modern-container {
            max-width: 1200px;
            margin: 48px auto 0 auto;
            padding: 2rem;
            background: #fff;
            border-radius: 16px;
            box-shadow: 0 8px 32px rgba(44, 62, 80, 0.08);
            border: 1px solid #eef4fb;
            animation: fadeIn 0.4s ease;
        }

        /* Titles and Buttons */
        .modern-title {
            font-size: 1.75rem;
            font-weight: 700;
            color: #2375D8;
            display: flex;
            align-items: center;
            gap: 0.8rem;
        }
        .modern-btn-primary {
            background: linear-gradient(90deg, #2375D8 0%, #0c9083 100%);
            color: #fff;
            font-weight: 600;
            font-size: 0.95rem;
            padding: 0.6rem 1.2rem;
            border: none;
            border-radius: 10px;
            box-shadow: 0 4px 16px rgba(36, 117, 216, 0.12);
            transition: all 0.2s ease;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            gap: 0.6rem;
            text-decoration: none;
        }
        .modern-btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 24px rgba(36, 117, 216, 0.18);
        }

        /* Form styles */
        .modern-card {
            background: #fdfdff;
            border-radius: 12px;
            border: 1px solid #eef4fb;
            padding: 1.5rem;
            margin-bottom: 2rem;
        }
        .modern-section-title {
            font-size: 1.1rem;
            color: #4a5568;
            margin-bottom: 1.2rem;
            font-weight: 600;
        }
        .modern-form-grid { display: grid; grid-template-columns: 1fr; gap: 1rem; }
        @media (min-width: 768px) {
            .modern-form-grid { grid-template-columns: 1fr 2fr; gap: 1.2rem 2rem; align-items: end; }
            .grid-col-span-2 { grid-column: span 2; }
        }
        .modern-info-item { display: flex; flex-direction: column; gap: 0.25rem; }
        .modern-info-label { font-size: 0.9rem; color: #718096; font-weight: 500; }
        .modern-info-value {
            font-size: 1rem;
            color: #2d3748;
            border: 1px solid #e3e6ed;
            border-radius: 8px;
            padding: 0.6rem 1rem;
            background: #fff;
            transition: border 0.2s;
        }
        .modern-info-value:focus { border-color: #2375D8; outline: none; box-shadow: 0 0 0 2px rgba(35, 117, 216, 0.1); }
        
        /* Table styles inspired by assignmentScores.ejs */
        .card {
            border-radius: 12px;
            border: 1px solid #eef4fb;
            overflow: hidden;
            background: #fff;
            box-shadow: 0 4px 16px rgba(44, 62, 80, 0.05);
            /* Thêm dòng dưới đây vào */
            width: 100%; 
            display: block;
        }
        .card-header { padding: 1rem 1.25rem; border-bottom: 1px solid #f1f6fb; background: linear-gradient(90deg, rgba(35,117,216,0.03), rgba(6,182,212,0.02)); }
        .card-body { padding: 0.5rem; }
        .table-responsive { overflow-x: auto; }
        .table { width:100%; border-collapse:collapse; }
        .table thead th { font-weight: 600; color: #475569; padding: .8rem 1rem; text-align: left; font-size: 0.9rem; background-color: #f8fafc; text-transform: uppercase; letter-spacing: 0.5px;}
        .table tbody tr { border-bottom: 1px solid #f1f5f9; }
        .table tbody tr:last-child { border-bottom: none; }
        .table tbody tr:hover { background-color: #f7faff; }
        .table tbody td { padding: .8rem 1rem; color: #475569; vertical-align: middle; font-size: 0.95rem; }
        
        /* Badge for status */
        .badge { padding: .25rem .6rem; border-radius: 999px; font-weight: 600; font-size: .8rem; }
        .bg-success { background-color: #dcfce7; color: #166534; }
        .bg-warning { background-color: #fef3c7; color: #92400e; }
        .unread-notification { cursor: pointer; }

        /* Actions */
        .actions { display:flex; gap:.5rem; align-items:center; justify-content: center; }
        .btn-delete {
            background: transparent;
            border: none;
            color: #ef4444;
            font-size: 1rem;
            cursor: pointer;
            padding: .2rem .4rem;
            border-radius: 6px;
            transition: all 0.2s;
        }
        .btn-delete:hover { background-color: #fee2e2; color: #b91c1c; }

        /* Animation */
        @keyframes fadeIn { from{opacity:0; transform:translateY(10px);} to{opacity:1; transform:none;} }

        /* DataTables overrides */
        .dataTables_wrapper .dataTables_length,
        .dataTables_wrapper .dataTables_filter { margin: 0.5rem; font-size: 0.9rem; }
        .dataTables_wrapper .dataTables_filter input { border-radius: 8px; padding: 0.4rem 0.8rem; border: 1px solid #e3e6ed; margin-left: 0.5rem; }
        .dataTables_wrapper .dataTables_length select { border-radius: 8px; padding: 0.4rem 0.8rem; border: 1px solid #e3e6ed; }
        .dataTables_wrapper .dataTables_paginate .paginate_button { padding: 0.4rem 0.8rem; border-radius: 8px; border: 1px solid transparent; }
        .dataTables_wrapper .dataTables_paginate .paginate_button.current { background: #2375D8; color: white !important; border-color: #2375D8; }
        .dataTables_wrapper .dataTables_paginate .paginate_button:hover { background: #eef4fb; border-color: #e3e6ed; color: #2375D8 !important; }
        .dataTables_wrapper .top { display: flex; justify-content: space-between; align-items: center; padding: 0.5rem 0.5rem 1rem 0.5rem; }
        .dataTables_wrapper .bottom { padding: 1rem 0.5rem 0.5rem 0.5rem; }
    </style>
</head>

<body>
    <%- include('../views/shared/header.ejs') %>
 
    <main>
        <div class="modern-container">
            <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem;">
                <div class="modern-title">
                    <i class="fas fa-bell"></i> Notifications
                </div>
                <% if (user.role === 'admin' || user.role === 'teacher') { %>
                <a href="#sendNotificationCard" class="modern-btn-primary" onclick="document.getElementById('sendNotificationCard').scrollIntoView({behavior:'smooth'});return false;">
                    <i class="fas fa-paper-plane"></i> Send Notification
                </a>
                <% } %>
            </div>

            <% if (user.role === 'admin' || user.role === 'teacher') { %>
            <div id="sendNotificationCard" class="modern-card">
                <div class="modern-section-title">
                    <i class="fas fa-paper-plane"></i> Send a New Notification
                </div>
                <form action="/notifications" method="POST" class="modern-form-grid">
                    <div class="modern-info-item">
                        <label for="userId" class="modern-info-label">To User</label>
                        <select name="userId" id="userId" class="modern-info-value" required>
                            <option value="">Select a user...</option>
                            <% users && users.forEach(u => { %>
                                <option value="<%= u.id %>"><%= u.full_name %> (<%= u.email %>)</option>
                            <% }) %>
                        </select>
                    </div>
                    <div class="modern-info-item grid-col-span-2">
                        <label for="message" class="modern-info-label">Message</label>
                        <input type="text" name="message" id="message" class="modern-info-value" required placeholder="Enter your notification message...">
                    </div>
                    <div class="grid-col-span-2" style="text-align:right;">
                        <button type="submit" class="modern-btn-primary"><i class="fas fa-paper-plane"></i> Send</button>
                    </div>
                </form>
            </div>
            <% } %>

            <div class="card">
                <div class="card-header">
                    <div style="font-weight:700; color:#0f1724;">All Notifications</div>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table" id="notificationsTable" style="width:100%;">
                            <thead>
                                <tr>
                                    <th style="width:40px;">ID</th>
                                    <th>From</th>
                                    <th>To</th>
                                    <th>Message</th>
                                    <th style="width:180px;">Date</th>
                                    <th style="width:100px; text-align: center;">Status</th>
                                    <th style="width:80px; text-align: center;">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% notifications.forEach(notification => { %>
                                <tr>
                                    <td><%= notification.id %></td>
                                    <td style="font-weight: 500;"><%= notification.sender_name %></td>
                                    <td style="font-weight: 500;"><%= notification.receiver_name %></td>
                                    <td><%= notification.message %></td>
                                    <td><%= new Date(notification.created_at).toLocaleString('en-US', { year: 'numeric', month: 'short', day: 'numeric', hour: '2-digit', minute: '2-digit' }) %></td>
                                    <td style="text-align: center;">
                                        <% if(notification.read) { %>
                                            <span class="badge bg-success">Read</span>
                                        <% } else { %>
                                            <span class="badge bg-warning unread-notification" data-id="<%= notification.id %>">Unread</span>
                                        <% } %>
                                    </td>
                                    <td>
                                        <div class="actions">
                                            <form method="POST" action="/notifications/<%= notification.id %>?_method=DELETE" style="display:inline;">
                                                <button type="submit" class="btn-delete" title="Delete" onclick="return confirm('Are you sure you want to delete this notification?')">
                                                    <i class="fas fa-trash"></i>
                                                </button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <%- include('../views/shared/footer.ejs') %>

    <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
    <script src="/js/script.js"></script>
    <script src="/js/main.js"></script>
    <script type="text/javascript" src="https://code.jquery.com/jquery-3.7.0.js"></script>
    <script type="text/javascript" src="https://cdn.datatables.net/1.13.7/js/jquery.dataTables.min.js"></script>
    <script type="text/javascript" src="https://cdn.datatables.net/responsive/2.5.0/js/dataTables.responsive.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#notificationsTable').DataTable({
                responsive: true,
                pageLength: 10,
                order: [[0, 'desc']],
                columnDefs: [ 
                    { targets: [6], orderable: false, searchable: false } 
                ],
                dom: '<"top"lf>rt<"bottom"ip>',
                language: {
                    search: "",
                    searchPlaceholder: "Search notifications...",
                    lengthMenu: "Show _MENU_",
                    info: "Showing _START_ to _END_ of _TOTAL_ entries",
                    paginate: {
                        first: "<<",
                        last: ">>",
                        next: ">",
                        previous: "<"
                    }
                }
            });

            $('#notificationsTable tbody').on('click', '.unread-notification', function () {
                const notificationId = $(this).data('id');
                const badge = $(this);

                $.post(`/notifications/${notificationId}/read`)
                    .done(function () {
                        badge
                            .removeClass('bg-warning unread-notification')
                            .addClass('bg-success')
                            .text('Read')
                            .css('cursor', 'default');
                    })
                    .fail(function (err) {
                        console.error('Error marking notification as read:', err);
                        alert('Failed to mark notification as read');
                    });
            });
        });
    </script>
</body>
</html>