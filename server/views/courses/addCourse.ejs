<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add New Course | Learning Management System</title>
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="stylesheet" href="/css/mobile.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <link rel="stylesheet" href="https://demos.creative-tim.com/notus-js/assets/styles/tailwind.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
    <link rel="stylesheet"
        href="https://demos.creative-tim.com/notus-js/assets/vendor/@fortawesome/fontawesome-free/css/all.min.css" />
    <style>
        html, body {
            height: 100%;
            margin: 0;
            padding: 0;
        }
        body {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        main, .modern-container {
            flex: 1 0 auto;
        }
        footer {
            flex-shrink: 0;
        }
        .modern-container {
            max-width: 1200px;
            margin: 48px auto 0 auto;
            padding: 2.5rem 2rem 2rem 2rem;
            background: #fff;
            border-radius: 18px;
            box-shadow: 0 4px 24px rgba(44,62,80,0.10);
            border: 1px solid #e3e6ed;
            animation: fadeIn 0.5s;
        }
        .modern-title {
            font-size: 2rem;
            font-weight: 700;
            color: #2375D8;
            text-align: center;
            margin-bottom: 2rem;
            letter-spacing: 1px;
        }
        .modern-form-group {
            margin-bottom: 1.5rem;
        }
        .modern-form-group label {
            display: block;
            font-weight: 600;
            color: #374151;
            margin-bottom: 0.5rem;
            letter-spacing: 0.5px;
        }
        .modern-form-control, .modern-form-control:focus {
            width: 100%;
            padding: 0.75rem 1rem;
            border: 1.5px solid #e5e7eb;
            border-radius: 8px;
            font-size: 1rem;
            background: #f9fafb;
            transition: border-color 0.2s, box-shadow 0.2s;
            outline: none;
            color: #22223b;
        }
        .modern-form-control:focus {
            border-color: #2375D8;
            box-shadow: 0 0 0 2px #2375d830;
            background: #fff;
        }
        .modern-btn-primary {
            width: 100%;
            background: linear-gradient(90deg, #2375D8 0%, #0c9083 100%);
            color: #fff;
            font-weight: 700;
            font-size: 1.1rem;
            padding: 0.85rem 0;
            border: none;
            border-radius: 10px;
            box-shadow: 0 4px 16px rgba(36,117,216,0.10);
            transition: background 0.2s, transform 0.15s;
            cursor: pointer;
            margin-top: 0.5rem;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.7rem;
        }
        .modern-btn-primary:hover {
            background: linear-gradient(90deg, #0c9083 0%, #2375D8 100%);
            transform: translateY(-2px) scale(1.03);
            box-shadow: 0 8px 24px rgba(36,117,216,0.18);
        }
        .modern-btn-secondary {
            width: 100%;
            background: #e2e8f0;
            color: #4a5568;
            font-weight: 600;
            font-size: 1.1rem;
            padding: 0.85rem 0;
            border: none;
            border-radius: 10px;
            transition: background 0.2s, color 0.2s;
            cursor: pointer;
            margin-top: 0.5rem;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.7rem;
            text-decoration: none;
            text-align: center;
        }
        .modern-btn-secondary:hover {
            background: #cbd5e0;
            color: #2375D8;
        }
        .modern-form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1.2rem;
        }
        .alert-box {
            padding: 1rem;
            margin-bottom: 1.5rem;
            border-radius: 8px;
            display: flex;
            align-items: center;
            gap: 0.75rem;
            font-weight: 500;
            animation: fadeIn 0.4s ease-out;
        }
        .alert-error {
            background-color: #fee2e2;
            color: #b91c1c;
            border: 1px solid #fca5a5;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(18px);}
            to { opacity: 1; transform: translateY(0);}
        }
    </style>
</head>

<body>
  <!-- HEADER -->
    <%- include('../shared/header') %>

    <main>
        <div class="modern-container">
            <div class="modern-title">
                <i class="fas fa-plus"></i> Add New Course
            </div>

            <div id="warning-container"></div>

            <form id="addCourseForm" action="/courses" method="POST">
                <div class="modern-form-group"> 
                    <label for="course_name">Course Name</label>
                    <input type="text" name="course_name" id="course_name" required class="modern-form-control">
                </div>

                <div class="modern-form-group">
                    <label for="description">Description</label>
                    <textarea name="description" id="description" rows="4" required placeholder="Course description" class="modern-form-control"></textarea>
                </div>

                <div class="modern-form-group">
                    <label for="tuition_fee">Tuition Fee (VND)</label>
                    <input type="number" name="tuition_fee" id="tuition_fee" min="0" step="1000" required placeholder="e.g. 2000000" class="modern-form-control" />
                </div>

                <div class="modern-form-row">
                    <div class="modern-form-group">
                        <label for="start_date">Start Date</label>
                        <input type="date" name="start_date" id="start_date" required class="modern-form-control" />
                    </div>
                    <div class="modern-form-group">
                        <label for="end_date">End Date</label>
                        <input type="date" name="end_date" id="end_date" required class="modern-form-control" />
                    </div>
                </div>

                <div style="display:flex; gap:1rem; margin-top:1.5rem;">
                    <a href="/courses" class="modern-btn-secondary"><i class="fas fa-arrow-left"></i> Cancel</a>
                    <button type="submit" class="modern-btn-primary"><i class="fas fa-plus"></i> Add Course</button>
                </div>
            </form>
<script src="/js/script.js"></script>
<script src="/js/main.js"></script>
            <script>
                const warningContainer = document.getElementById('warning-container');
                function showWarning(message) {
                    warningContainer.innerHTML = `<div class="alert-box alert-error"><i class="fas fa-exclamation-triangle fa-lg"></i><span>${message}</span></div>`;
                }
                function clearWarnings(){ warningContainer.innerHTML = ''; }

                document.getElementById('addCourseForm').addEventListener('submit', function(e){
                    clearWarnings();
                    const startDate = new Date(document.getElementById('start_date').value);
                    const endDate = new Date(document.getElementById('end_date').value);
                    if (isNaN(startDate) || isNaN(endDate)) {
                        e.preventDefault();
                        showWarning('Please provide valid start and end dates.');
                        return;
                    }
                    if (endDate <= startDate) {
                        e.preventDefault();
                        showWarning('End date must be after start date');
                        return;
                    }
                });
            </script>
        </div>
    </main>

    <footer class="relative bg-blueGray-200 pt-8 pb-6">
        <div class="container mx-auto px-4">
            <div class="flex flex-wrap text-left lg:text-left">
                <div class="w-full lg:w-6/12 px-4">
                    <h4 class="text-3xl fonat-semibold text-blueGray-700">Liên hệ với chúng tôi!</h4>
                    <h5 class="text-lg mt-0 mb-2 text-blueGray-600">
                        Tìm kiếm chúng tôi trên bất kỳ mạng xã hội nào và gửi cho chúng tôi một tin nhắn! Chúng tôi sẽ trả lời bạn
                        ngay khi có thể.
                    </h5>
                    <div class="mt-6 lg:mb-0 mb-6">
                        <button
                            class="bg-white text-lightBlue-400 shadow-lg font-normal h-10 w-10 items-center justify-center align-center rounded-full outline-none focus:outline-none mr-2"
                            type="button">
                            <i class="fab fa-twitter"></i></button><button
                            class="bg-white text-lightBlue-600 shadow-lg font-normal h-10 w-10 items-center justify-center align-center rounded-full outline-none focus:outline-none mr-2"
                            type="button">
                            <i class="fab fa-facebook-square"></i></button><button
                            class="bg-white text-pink-400 shadow-lg font-normal h-10 w-10 items-center justify-center align-center rounded-full outline-none focus:outline-none mr-2"
                            type="button">
                            <i class="fab fa-dribbble"></i></button><button
                            class="bg-white text-blueGray-800 shadow-lg font-normal h-10 w-10 items-center justify-center align-center rounded-full outline-none focus:outline-none mr-2"
                            type="button">
                            <i class="fab fa-github"></i>
                        </button>
                    </div>
                </div>
                <div class="w-full lg:w-6/12 px-4">
                    <div class="flex flex-wrap items-top mb-6">
                        <div class="w-full lg:w-4/12 px-4 ml-auto">
                            <span class="block uppercase text-blueGray-500 text-sm font-semibold mb-2">Bạn Đang Tìm Kiếm</span>
                            <ul class="list-unstyled">
                                <li>
                                    <a class="text-blueGray-600 hover:text-blueGray-800 font-semibold block pb-2 text-sm" href="">Liên
                                        Hệ</a>
                                </li>
                                <li>
                                    <a class="text-blueGray-600 hover:text-blueGray-800 font-semibold block pb-2 text-sm" href="">Thông
                                        Tin Mới</a>
                                </li>
                                <li>
                                    <a class="text-blueGray-600 hover:text-blueGray-800 font-semibold block pb-2 text-sm" href="">Tuyển
                                        Dụng</a>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
            <hr class="my-6 border-blueGray-300">
            <div class="flex flex-wrap items-center md:justify-between justify-center">
                <div class="w-full md:w-4/12 px-4 mx-auto text-center">
                    <div class="text-sm text-blueGray-500 font-semibold py-1">
                        Copyright © <span id="get-current-year">2025</span><a href="https://www.facebook.com/ttgdvpttnht"
                            class="text-blueGray-500 hover:text-gray-800" target="_blank"> Talent Education</a>. Made by
                        <a href="https://github.com/Rin-0808" class="text-blueGray-500 hover:text-blueGray-800">Rin</a>.
                    </div>
                </div>
            </div>
        </div>
    </footer>
</body>

</html>