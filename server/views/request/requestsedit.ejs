<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Request</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/mobile.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://demos.creative-tim.com/notus-js/assets/styles/tailwind.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
    <link rel="stylesheet"
        href="https://demos.creative-tim.com/notus-js/assets/vendor/@fortawesome/fontawesome-free/css/all.min.css">
    <style>
        .modern-container {
            max-width: 1200px;
            margin: 48px auto;
            padding: 2.25rem 2rem;
            background: #fff;
            border-radius: 14px;
            box-shadow: 0 6px 30px rgba(44,62,80,0.08);
            border: 1px solid #e6e9ef;
            animation: fadeIn 0.4s ease;
        }
        .modern-title {
            font-size: 1.6rem;
            font-weight: 700;
            color: #2375D8;
            margin-bottom: 1.25rem;
            display:flex;
            align-items:center;
            gap:0.6rem;
        }
        .modern-form-group { margin-bottom: 1.1rem; }
        .modern-form-group label { display:block; margin-bottom:0.45rem; font-weight:600; color:#374151; }
        .modern-form-control {
            width:100%;
            padding:0.7rem;
            border:1px solid #e2e8f0;
            border-radius:8px;
            font-size:1rem;
        }
        .modern-form-control[readonly] {
            background-color: #f8f9fa;
            cursor: not-allowed;
        }
        .modern-btn-primary, .modern-btn-secondary {
            font-weight:700;
            padding:0.85rem 1rem;
            border:none;
            border-radius:10px;
            cursor:pointer;
            display:inline-flex;
            gap:0.6rem;
            align-items:center;
            justify-content:center;
            text-decoration: none;
        }
        .modern-btn-primary {
            background: linear-gradient(90deg, #2375D8 0%, #0c9083 100%);
            color: #fff;
        }
        .modern-btn-secondary {
            background:#f1f5f9;
            color:#475569;
        }
        @keyframes fadeIn { from { opacity:0; transform:translateY(10px);} to { opacity:1; transform:translateY(0);} }
    </style>
</head>

<body>
    <%- include('../shared/header.ejs') %>

<main>
    <div class="modern-container">
        <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:1.5rem;">
            <div class="modern-title">
                <i class="fas fa-edit"></i>
                <%= title %>
            </div>
            <div>
                <a href="/requests" class="modern-btn-secondary"><i class="fas fa-arrow-left"></i> Back</a>
            </div>
        </div>

        <form id="editForm">
            <div class="modern-form-group">
                <label>Request Type</label>
                <input type="text" class="modern-form-control" value="<%= request.type_name %>" readonly>
            </div>

            <div class="modern-form-group">
                <label for="classId">Related Class (Optional)</label>
                <select name="classId" id="classId" class="modern-form-control">
                    <option value="">None</option>
                    <% classes.forEach(function(class_) { %>
                    <option value="<%= class_.id %>" <%= request.class_id === class_.id ? 'selected' : '' %>>
                        <%= class_.class_name %>
                    </option>
                    <% }); %>
                </select>
            </div>

            <div class="modern-form-group">
                <label for="details">Details</label>
                <textarea name="details" id="details" rows="5" class="modern-form-control" required><%= request.description %></textarea>
            </div>

            <div style="display:flex; justify-content:flex-end; gap:1rem; margin-top:2rem;">
                <a href="/requests" class="modern-btn-secondary"><i class="fas fa-times"></i> Cancel</a>
                <button type="submit" class="modern-btn-primary"><i class="fas fa-save"></i> Update Request</button>
            </div>
        </form>
    </div>
</main>

    <script>
        document.getElementById('editForm').addEventListener('submit', async function (e) {
            e.preventDefault();
            try {
                const response = await fetch('/requestEdit/<%= request.request_id %>', {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        details: this.details.value,
                        classId: this.classId.value || null
                    })
                });

                if (response.ok) {
                    window.location.href = '/requests';
                } else {
                    alert((await response.json()).error || 'Failed to update request');
                }
            } catch (error) {
                alert('Error updating request');
            }
        });
    </script>

<%- include('../shared/footer.ejs') %>
</body>

</html>